@extends('layouts.admin')
@section('title','Sales Target')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><p class="text-muted mb-0">Party-wise product category targets</p></div><div><a href="{{ route('admin.sales-targets.report') }}" class="btn btn-outline-primary mr-2"><i class="fas fa-chart-line mr-1"></i> Target Report</a>@can('sales_targets.create')<a href="{{ route('admin.sales-targets.create') }}" class="btn btn-primary"><i class="fas fa-plus mr-1"></i> Set Target</a>@endcan</div></div>
<div class="card shadow-sm"><div class="card-body"><div class="table-responsive"><table class="table table-hover" id="targetsTable"><thead><tr><th>Party</th><th>Period</th><th>Dates</th><th>Product Categories & Goals</th><th>Status</th><th></th></tr></thead><tbody>@foreach($targets as $target)<tr><td><strong>{{ $target->party?->display_name ?? 'Cash / Walk-in' }}</strong></td><td>{{ ucfirst(str_replace('_',' ',$target->period_type)) }}</td><td>{{ $target->starts_on->format('d M Y') }}<br>to {{ $target->ends_on->format('d M Y') }}</td><td>@foreach($target->items as $item)<span class="badge badge-light mr-1 mb-1">{{ $item->productCategory?->name }}: {{ number_format($item->target_value,2) }} {{ $item->target_type }}</span>@endforeach</td><td><span class="badge badge-{{ $target->status === 'active' ? 'success' : 'secondary' }}">{{ ucfirst($target->status) }}</span></td><td class="text-nowrap">@can('sales_targets.edit')<a href="{{ route('admin.sales-targets.edit',$target) }}" class="btn btn-sm btn-outline-primary"><i class="fas fa-edit"></i></a>@endcan @can('sales_targets.delete')<form class="d-inline" method="POST" action="{{ route('admin.sales-targets.destroy',$target) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger btn-delete"><i class="fas fa-trash"></i></button></form>@endcan</td></tr>@endforeach</tbody></table></div></div></div>
@endsection
@push('scripts')<script>$('#targetsTable').DataTable({pageLength:25,order:[[2,'desc']]});</script>@endpush
